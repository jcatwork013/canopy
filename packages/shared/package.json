{
  "name": "@canopy/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Platform-agnostic types + API client shared by the web PWA and the React Native app.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./api": "./src/api/index.ts",
    "./types": "./src/types/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "lint": "echo 'no linter configured'"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
